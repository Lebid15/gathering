# مشروع التجمع - نسخة HTML
## Gathering Project - HTML Version

مشروع لإدارة وعرض معلومات الأعضاء باستخدام HTML + JavaScript + CSS مع Supabase كقاعدة بيانات.

---

## 📋 نظرة عامة

هذا المشروع يتكون من:
- **صفحات عامة**: يمكن لأي شخص الوصول إليها
- **لوحة تحكم الإدارة**: مخصصة للمدير فقط

---

## 🎯 المميزات

### للمستخدمين العاديين:
- ✅ عرض قائمة الأعضاء المرئيين
- ✅ البحث عن الأعضاء
- ✅ واجهة سهلة ومريحة

### للمدير:
- ✅ تسجيل دخول آمن
- ✅ إضافة أعضاء جدد
- ✅ تعديل بيانات الأعضاء
- ✅ حذف الأعضاء
- ✅ إخفاء/إظهار الأعضاء في القائمة العامة
- ✅ عرض الإحصائيات

---

## 📁 هيكل المشروع

```
html-version/
├── index.html              # الصفحة الرئيسية
├── members.html            # صفحة عرض الأعضاء (عامة)
├── about.html              # صفحة عن المشروع
├── admin-login.html        # صفحة تسجيل دخول الإدارة
├── admin-dashboard.html    # لوحة تحكم الإدارة
├── css/
│   └── styles.css         # ملف التصميم
└── js/
    ├── supabase-config.js # إعدادات Supabase
    ├── api.js             # دوال الـ API
    ├── auth.js            # المصادقة
    ├── home.js            # الصفحة الرئيسية
    ├── members.js         # صفحة الأعضاء
    └── admin.js           # لوحة التحكم
```

---

## 🚀 البدء السريع

### 1. إعداد Supabase

#### أ) إنشاء مشروع Supabase:
1. اذهب إلى [https://supabase.com](https://supabase.com)
2. أنشئ حساب جديد أو سجل دخول
3. أنشئ مشروع جديد (New Project)
4. احفظ الـ URL والـ Anon Key

#### ب) إنشاء جدول الأعضاء:
افتح SQL Editor في Supabase وقم بتنفيذ:

```sql
-- إنشاء جدول الأعضاء
CREATE TABLE members (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    first_name VARCHAR(100) NOT NULL,
    last_name VARCHAR(100) NOT NULL,
    email VARCHAR(255),
    phone_primary VARCHAR(20),
    phone_primary_country_code VARCHAR(5),
    phone_secondary VARCHAR(20),
    phone_secondary_country_code VARCHAR(5),
    city VARCHAR(100),
    country VARCHAR(100),
    is_visible BOOLEAN DEFAULT true,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- إضافة فهرس للبحث
CREATE INDEX idx_members_name ON members(first_name, last_name);
CREATE INDEX idx_members_visible ON members(is_visible);

-- تفعيل Row Level Security (اختياري)
ALTER TABLE members ENABLE ROW LEVEL SECURITY;

-- سياسة لقراءة الأعضاء المرئيين (للجميع)
CREATE POLICY "Anyone can view visible members"
ON members FOR SELECT
USING (is_visible = true);

-- سياسة لإدارة الأعضاء (بدون مصادقة - استخدم localStorage للتحكم)
CREATE POLICY "Enable all access for authenticated users"
ON members FOR ALL
USING (true)
WITH CHECK (true);
```

#### ج) إدخال بيانات تجريبية (اختياري):
```sql
INSERT INTO members (first_name, last_name, email, phone_primary, city, country, is_visible)
VALUES 
    ('أحمد', 'محمد', 'ahmed@example.com', '0501234567', 'الرياض', 'السعودية', true),
    ('فاطمة', 'علي', 'fatima@example.com', '0507654321', 'جدة', 'السعودية', true),
    ('محمد', 'خالد', 'mohammed@example.com', '0509876543', 'الدمام', 'السعودية', false);
```

---

### 2. إعداد المشروع

#### أ) تحديث إعدادات Supabase:
افتح ملف `js/supabase-config.js` وقم بتحديث:

```javascript
const SUPABASE_URL = 'https://YOUR_PROJECT_ID.supabase.co';
const SUPABASE_ANON_KEY = 'YOUR_ANON_KEY_HERE';
```

#### ب) إضافة مكتبة Supabase:
أضف هذا السطر في جميع ملفات HTML قبل تحميل سكريبتات المشروع:

```html
<!-- قبل </body> -->
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script type="module" src="js/supabase-config.js"></script>
```

---

### 3. تشغيل المشروع

#### الطريقة الأولى - باستخدام Live Server (الأفضل):
1. افتح VSCode
2. ثبت إضافة "Live Server"
3. انقر بالزر الأيمن على `index.html`
4. اختر "Open with Live Server"

#### الطريقة الثانية - باستخدام Python:
```bash
cd frontend/html-version
python -m http.server 8000
```
ثم افتح المتصفح على: `http://localhost:8000`

#### الطريقة الثالثة - فتح الملف مباشرة:
- افتح ملف `index.html` في المتصفح مباشرة
- ⚠️ قد لا يعمل CORS بشكل صحيح في بعض المتصفحات

---

## 🔐 بيانات تسجيل دخول الإدارة

```
رقم الجوال: 5443805422
كلمة السر: Imadelabes2025
```

⚠️ **هام**: غيّر هذه البيانات في ملف `js/auth.js` قبل النشر على الإنترنت!

---

## 📄 الصفحات

### 1. الصفحة الرئيسية (`index.html`)
- معلومات عن المشروع
- إحصائيات عامة
- روابط سريعة

### 2. صفحة الأعضاء (`members.html`)
- عرض جميع الأعضاء المرئيين
- البحث عن الأعضاء
- معلومات العضو (الاسم، البريد، الجوال، المدينة)

### 3. صفحة عن المشروع (`about.html`)
- معلومات تفصيلية عن المشروع
- التقنيات المستخدمة
- المميزات

### 4. صفحة تسجيل دخول الإدارة (`admin-login.html`)
- نموذج تسجيل دخول بسيط
- التحقق من البيانات

### 5. لوحة التحكم (`admin-dashboard.html`)
- إدارة كاملة للأعضاء
- إضافة/تعديل/حذف
- إخفاء/إظهار الأعضاء
- إحصائيات مفصلة

---

## 🛠️ التقنيات المستخدمة

- **HTML5**: بناء الصفحات
- **CSS3**: التصميم
- **Bootstrap 5**: إطار العمل
- **JavaScript (Vanilla)**: البرمجة
- **Supabase**: قاعدة البيانات
- **Bootstrap Icons**: الأيقونات

---

## 📝 ملاحظات مهمة

### الأمان:
1. **بيانات الإدارة**: يتم حفظها في `localStorage` - هذا ليس آمن 100%
2. **للإنتاج**: استخدم Supabase Auth أو JWT للمصادقة الحقيقية
3. **Row Level Security**: فعّل سياسات أمان أقوى في Supabase

### الأداء:
- استخدم CDN للمكتبات
- ضغط ملفات CSS و JS قبل النشر
- استخدم صور محسنة

### التوافق:
- يعمل على جميع المتصفحات الحديثة
- متجاوب مع جميع الأجهزة (موبايل، تابلت، سطح المكتب)

---

## 🔧 التخصيص

### تغيير الألوان:
عدّل ملف `css/styles.css`:
```css
.bg-primary {
    background: linear-gradient(135deg, #YOUR_COLOR1, #YOUR_COLOR2) !important;
}
```

### تغيير بيانات الأدمن:
عدّل ملف `js/auth.js`:
```javascript
const ADMIN_PHONE = 'YOUR_PHONE';
const ADMIN_PASSWORD = 'YOUR_PASSWORD';
```

### إضافة حقول جديدة:
1. أضف الحقل في جدول Supabase
2. عدّل النماذج في HTML
3. حدّث دوال API في `js/api.js`

---

## 🐛 حل المشاكل

### المشكلة: "Supabase is not defined"
**الحل**: تأكد من إضافة سكريبت Supabase قبل سكريبتات المشروع:
```html
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
```

### المشكلة: "لا تظهر البيانات"
**الحل**: 
1. تحقق من إعدادات Supabase في `js/supabase-config.js`
2. افتح Console في المتصفح وابحث عن الأخطاء
3. تأكد من وجود بيانات في جدول `members`

### المشكلة: "CORS Error"
**الحل**: استخدم Live Server أو HTTP Server، لا تفتح الملف مباشرة

---

## 📦 النشر

### على Netlify:
1. ارفع مجلد `html-version` إلى GitHub
2. اذهب إلى [Netlify](https://www.netlify.com)
3. اربط المستودع وانشر

### على Vercel:
1. ارفع المجلد إلى GitHub
2. اذهب إلى [Vercel](https://vercel.com)
3. استورد المشروع وانشر

### على GitHub Pages:
1. ارفع المجلد إلى GitHub
2. اذهب إلى Settings → Pages
3. فعّل GitHub Pages من الفرع الرئيسي

---

## 🤝 المساهمة

إذا أردت تحسين المشروع:
1. Fork المستودع
2. أنشئ فرع جديد
3. قم بالتعديلات
4. أرسل Pull Request

---

## 📧 الدعم

للاستفسارات أو المساعدة، تواصل مع فريق التطوير.

---

## 📜 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

## ✨ شكر خاص

- Bootstrap لإطار العمل الرائع
- Supabase لقاعدة البيانات
- Bootstrap Icons للأيقونات الجميلة

---

**تم بناء المشروع بـ ❤️ في 2025**
